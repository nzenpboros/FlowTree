import "./styles/styles.css";

const SortButton = () => {
    const handleClick = () => {

    }
    
    return (
        <button onClick={handleClick}>sort</button>
    )
}

export default SortButton;