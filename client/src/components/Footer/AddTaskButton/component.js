import "./styles/styles.css";

const AddTaskButton = () => {
    const handleClick = () => {

    }

    return (
        <button classname="button"
                onClick={handleClick}
                id="addTaskButton"
        >+</button>
    )
}

export default AddTaskButton;