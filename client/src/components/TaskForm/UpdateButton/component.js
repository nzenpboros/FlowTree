import "./styles/styles.css";

const UpdateButton = ({full}) => {
    const handleClick = () => {

    }

    //return small button or large button
    
    return (
        <button onClick={handleClick} width={full}>Update</button>
    )
}

export default UpdateButton;